// Centralized Next.js config. The basePath keeps the app mounted under /RCMStudio
/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/rcm-studio',
};

export default nextConfig;
